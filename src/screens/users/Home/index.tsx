import { Categories, StoreListings } from "@/components/Home";
const Home = () => {
  return (
    <section className="w-full">
      <Categories />
      <StoreListings />
    </section>
  );
};

export default Home;
